//Implemente um programa que realize o swap de suas variáveis inteiras passadas como parâmetro. Use ponteiros.
#include <stdio.h>

int main(void) {
  int alfa, beta, aux;

  a = &alfa;
  b = &beta;

  aux = *a;
  *a = *b;
  *b = aux;

  return 0;

}
